import { AbsoluteFill, staticFile } from "remotion";

const Souvenirs = () => {
    const listWords = ["des moments inoubliables", "des souvenirs", "du fun"];
    
    // Anim : Mais Crobot'ic, c'est aussi : this with Typing, and then same animation than adjective with listWords
    // Anim : Take all place and centered and then resize and put on the top
    // Anim : See all photos !
    return (
        <AbsoluteFill>
            
        </AbsoluteFill>
    )
}

export default Souvenirs;